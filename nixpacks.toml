# Only override start - let nixpacks auto-detect Python
[start]
cmd = "cp -rn teleband/media/* /app/mediafiles/ 2>/dev/null; python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
