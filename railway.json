{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "export DJANGO_SETTINGS_MODULE=config.settings.railway && echo '=== DEBUG: pwd ===' && pwd && echo '=== DEBUG: ls mediafiles ===' && ls -la /app/mediafiles/ || echo 'mediafiles not found' && echo '=== DEBUG: ls teleband/media ===' && ls teleband/media/ || echo 'teleband/media not found' && echo '=== DEBUG: copying ===' && cp -rv teleband/media/* /app/mediafiles/ || echo 'copy failed' && echo '=== DEBUG: ls mediafiles after ===' && ls -la /app/mediafiles/ || echo 'still not found' && python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
